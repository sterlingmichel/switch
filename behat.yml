default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    kernel: '@kernel'
                    simpleArg: 'string'
                    session: '@session'

    extensions:
        Behat\Symfony2Extension:
            kernel:
                bootstrap: features/bootstrap/bootstrap.php
                class: App\Kernel
